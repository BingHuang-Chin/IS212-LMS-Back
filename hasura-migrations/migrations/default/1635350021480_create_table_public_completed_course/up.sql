CREATE TABLE "public"."completed_course" ("learner_id" integer NOT NULL, "badge_id" integer NOT NULL, "course_id" integer NOT NULL, "course_completion_date" time NOT NULL, PRIMARY KEY ("learner_id","course_id","badge_id") , FOREIGN KEY ("badge_id") REFERENCES "public"."badge"("id") ON UPDATE cascade ON DELETE cascade);COMMENT ON TABLE "public"."completed_course" IS E'store history of completed courses';
