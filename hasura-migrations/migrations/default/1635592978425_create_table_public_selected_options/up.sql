CREATE TABLE "public"."selected_options" ("option_id" integer NOT NULL, "quiz_id" integer NOT NULL, "learner_id" integer NOT NULL, "attempt" integer NOT NULL, PRIMARY KEY ("option_id","quiz_id","learner_id","attempt") , FOREIGN KEY ("quiz_id", "learner_id", "attempt") REFERENCES "public"."completed_quiz"("quiz_id", "learner_id", "attempt") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("option_id") REFERENCES "public"."question_option"("id") ON UPDATE cascade ON DELETE cascade);
